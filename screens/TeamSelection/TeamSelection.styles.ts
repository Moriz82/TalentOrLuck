import {StyleSheet} from 'react-native';

const TeamSelectionStyles = StyleSheet.create({
  safeAreaViewStyle: {
    flex: 1,
    backgroundColor: '#222222',
  },
});

export default TeamSelectionStyles;
